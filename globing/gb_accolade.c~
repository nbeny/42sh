#include "globing.h"

t_new	*make_pointpoint(t_new *new, char *str)
{
	t_new	*s;
	int		c;

	c = str[0];
	s = new;
	if (new != NULL)
	{
		while (s != NULL)
		{
			while (c <= str[3])
			{
				
				c++;
			}
		}
	}
	else
	{
		
	}
	return (new);
}
